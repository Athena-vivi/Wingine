# Builder

Internal Builder Console MVP for moving a selected problem through:

`Problem -> Workflow -> Capabilities -> Output`

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000/builder-console`.
